# Report Archive

This folder holds historical one-off reports and logs that should not clutter the active report root.

- `benchmark_reports_20260424/`: old network-signal benchmark report bundles and plots.
- `logs/`: standalone logs that predate the current `reports/experiment_logs/` layout.

Use active reports under `reports/leaderboards/`, `reports/latest/`, `reports/training/`, `reports/experiment_logs/`, and `reports/experiment_suites/` for current analysis.

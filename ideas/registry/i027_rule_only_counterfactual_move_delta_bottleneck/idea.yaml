idea_id: i027
name: Rule-Only Counterfactual Move-Delta Bottleneck
slug: rule_only_counterfactual_move_delta_bottleneck
status: implemented
created_at: '2026-04-30T15:43:30+00:00'
author: Codex
short_thesis: '- Idea name: Rule-Only Counterfactual Move-Delta Bottleneck, abbreviated
  `CDBN`. - One-sentence thesis: A position is puzzle-like when the side-to-move''s
  rule-only one...'
novelty_claim: Promoted from `ideas/research/packets/classic/chess_nn_research_2026-04-21_0436_tuesday_los_angeles_move_delta_bottleneck.md`;
  bespoke deterministic pseudo-legal move-delta enumerator combined with a sparse
  permutation-invariant move-cone bottleneck (sparsemax/entmax-1.5 attention plus
  mean/second-moment/kappa statistics) over learned counterfactual response vectors.
expected_advantage: Better near-puzzle recall at matched non-puzzle false-positive
  rate by exposing sparse anisotropic one-ply counterfactual structure without engine
  leakage.
target_task: 'puzzle_binary classification: fine labels 0 and 1 map to non-puzzle,
  fine label 2 maps to puzzle.'
input_representation: Current-board simple_18 tensor only; CRTK/source metadata is
  reporting-only and never used as model input.
output_heads: One puzzle logit plus move-cone bottleneck diagnostics (kappa, sparse
  active count, alpha entropy/peak, response moment norms, pseudo-legal move count,
  capture/promotion fractions) saved to prediction artifacts.
compute_notes: Compact convolutional context encoder, deterministic pseudo-legal delta
  enumerator, and a sparsemax/entmax-1.5 move-cone bottleneck over learned move responses.
implementation_status: implemented
trainer_entrypoint: ideas/registry/i027_rule_only_counterfactual_move_delta_bottleneck/train.py
config_path: ideas/registry/i027_rule_only_counterfactual_move_delta_bottleneck/config.yaml
model_path: ideas/registry/i027_rule_only_counterfactual_move_delta_bottleneck/model.py
latest_result_path: null
notes: Bespoke implementation of the markdown architecture. The idea-local model wrapper
  delegates to `src/chess_nn_playground/models/counterfactual_delta_bottleneck.py`.
  Reuses the rule-only simple_18 adapter and pseudo-legal delta enumerator from i025
  but replaces the energy-based pool with a sparsemax/entmax-1.5 move-cone bottleneck
  plus mean/second-moment/kappa statistics.
implementation_kind: bespoke_model

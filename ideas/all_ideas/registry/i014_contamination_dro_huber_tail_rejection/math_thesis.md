# Mathematical Thesis

Fine-label `1` positions are binary negatives but are the benchmark's main false-positive pressure. The loss adds a Huberized one-sided residual `relu(logit + margin)` on those examples and estimates its capped upper tail.

The Huber cap bounds individual influence, while the upper-tail mean focuses training on near-puzzle negatives that the current classifier most wants to call puzzles.

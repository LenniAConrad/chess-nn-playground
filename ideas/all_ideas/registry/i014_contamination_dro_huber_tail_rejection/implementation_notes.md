# Implementation Notes

- Model builder: `contamination_dro_huber_tail_rejection`
- Loss: `contamination_dro_huber`
- Fine labels are consumed only by the loss, not by the model.
- No engine, source, file, verification, or PV metadata is accepted by the model.

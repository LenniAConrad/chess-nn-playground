idea_id: i239
name: Permanent Ryser Coupling Network
slug: permanent_ryser_network
status: implemented
created_at: '2026-05-05T17:00:00+00:00'
author: Claude
short_thesis: "Builds top-k attacker x top-k defender bilinear interaction matrix M (k=6); computes its permanent via Ryser's formula -- the unsigned count of perfect attacker-to-defender matchings. Distinct from DPP (signed determinant, i058) and Pfaffian (signed matchings of skew-graphs, i226)."
novelty_claim: Bespoke `nn.Module` implementation in `src/chess_nn_playground/models/permanent_ryser.py`; routed via the standard
  registry (not the generic ResearchPacketProbe).
expected_advantage: Hand-coded mechanism implements the algebra exactly per the
  source packet -- no profile-routing approximation.
target_task: 'puzzle_binary classification: fine labels 0 and 1 map to non-puzzle,
  fine label 2 maps to puzzle.'
input_representation: Current-board simple_18 tensor only; CRTK/source metadata is
  reporting-only and never used as model input.
output_heads: One puzzle logit (squeezed) for binary BCE.
compute_notes: "Ryser at k=6: 64 subset iterations of inclusion-exclusion; fully differentiable."
implementation_status: implemented
trainer_entrypoint: ideas/i239_permanent_ryser_network/train.py
config_path: ideas/i239_permanent_ryser_network/config.yaml
model_path: ideas/i239_permanent_ryser_network/model.py
latest_result_path: null
notes: Bespoke implementation -- the algebraic operator described in the source
  packet is realized as a hand-written torch.nn.Module class
  `PermanentRyserNetwork` and registered under model name `permanent_ryser_network` in
  `src/chess_nn_playground/models/registry.py`.
mechanism_family: linear_algebra

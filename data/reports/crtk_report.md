# crtk Inspection

- Found: `True`
- Executable: `/usr/local/bin/crtk`

## crtk version

```text
Unknown command: --version
crtk — ChessRTK (chess research toolkit)

usage:
  crtk <area> <action> [options] [args]
  crtk help [command...]

areas:
  record          Export, filter, split, and summarize .record files
  fen             Validate, normalize, generate, print, and transform FENs
  move            List, convert, and apply moves
  engine          Analyze, evaluate, search, and run movegen checks
  book            Render chess books, covers, and diagram PDFs
  puzzle          Mine, convert, tag, and summarize puzzle lines
  config          Show/validate configuration
  gui             Launch the GUI
  gui-web         Launch the chess-web-inspired GUI
  gui-next        Launch the Studio GUI v3
  doctor          Check Java, config, protocol, engine, and local artifacts
  clean           Delete session cache/logs
  help            Show command help

command style:
  Use noun groups plus actions, for example `crtk move list` or `crtk engine bestmove`.
  Prefer named flags for structured values: `--fen`, `--input`, `--output`, `--format`.
  Put options before free-form args when scripting, and use `--` if a value could look like a flag.

help:
  crtk --help                Show this summary
  crtk move --help           Show help for one area
  crtk move list --help      Show help for one command
  crtk help --full           Show the built-in command reference


```

## crtk help

```text
crtk — ChessRTK (chess research toolkit)

usage:
  crtk <area> <action> [options] [args]
  crtk help [command...]

areas:
  record          Export, filter, split, and summarize .record files
  fen             Validate, normalize, generate, print, and transform FENs
  move            List, convert, and apply moves
  engine          Analyze, evaluate, search, and run movegen checks
  book            Render chess books, covers, and diagram PDFs
  puzzle          Mine, convert, tag, and summarize puzzle lines
  config          Show/validate configuration
  gui             Launch the GUI
  gui-web         Launch the chess-web-inspired GUI
  gui-next        Launch the Studio GUI v3
  doctor          Check Java, config, protocol, engine, and local artifacts
  clean           Delete session cache/logs
  help            Show command help

command style:
  Use noun groups plus actions, for example `crtk move list` or `crtk engine bestmove`.
  Prefer named flags for structured values: `--fen`, `--input`, `--output`, `--format`.
  Put options before free-form args when scripting, and use `--` if a value could look like a flag.

help:
  crtk --help                Show this summary
  crtk move --help           Show help for one area
  crtk move list --help      Show help for one command
  crtk help --full           Show the built-in command reference


```

## Selected command help

### help_record-to-csv

```text
Unknown command for help: record-to-csv
crtk — ChessRTK (chess research toolkit)

usage:
  crtk <area> <action> [options] [args]
  crtk help [command...]

areas:
  record          Export, filter, split, and summarize .record files
  fen             Validate, normalize, generate, print, and transform FENs
  move            List, convert, and apply moves
  engine          Analyze, evaluate, search, and run movegen checks
  book            Render chess books, covers, and diagram PDFs
  puzzle          Mine, convert, tag, and summarize puzzle lines
  config          Show/validate configuration
  gui             Launch the GUI
  gui-web         Launch the chess-web-inspired GUI
  gui-next        Launch the Studio GUI v3
  doctor          Check Java, config, protocol, engine, and local artifacts
  clean           Delete session cache/logs
  help            Show command help

command style:
  Use noun groups plus actions, for example `crtk move list` or `crtk engine bestmove`.
  Prefer named flags for structured values: `--fen`, `--input`, `--output`, `--format`.
  Put options before free-form args when scripting, and use `--` if a value could look like a flag.

help:
  crtk --help                Show this summary
  crtk move --help           Show help for one area
  crtk move list --help      Show help for one command
  crtk help --full           Show the built-in command reference


```

### help_record-to-plain

```text
Unknown command for help: record-to-plain
crtk — ChessRTK (chess research toolkit)

usage:
  crtk <area> <action> [options] [args]
  crtk help [command...]

areas:
  record          Export, filter, split, and summarize .record files
  fen             Validate, normalize, generate, print, and transform FENs
  move            List, convert, and apply moves
  engine          Analyze, evaluate, search, and run movegen checks
  book            Render chess books, covers, and diagram PDFs
  puzzle          Mine, convert, tag, and summarize puzzle lines
  config          Show/validate configuration
  gui             Launch the GUI
  gui-web         Launch the chess-web-inspired GUI
  gui-next        Launch the Studio GUI v3
  doctor          Check Java, config, protocol, engine, and local artifacts
  clean           Delete session cache/logs
  help            Show command help

command style:
  Use noun groups plus actions, for example `crtk move list` or `crtk engine bestmove`.
  Prefer named flags for structured values: `--fen`, `--input`, `--output`, `--format`.
  Put options before free-form args when scripting, and use `--` if a value could look like a flag.

help:
  crtk --help                Show this summary
  crtk move --help           Show help for one area
  crtk move list --help      Show help for one command
  crtk help --full           Show the built-in command reference


```

### help_records

```text
Unknown command for help: records
crtk — ChessRTK (chess research toolkit)

usage:
  crtk <area> <action> [options] [args]
  crtk help [command...]

areas:
  record          Export, filter, split, and summarize .record files
  fen             Validate, normalize, generate, print, and transform FENs
  move            List, convert, and apply moves
  engine          Analyze, evaluate, search, and run movegen checks
  book            Render chess books, covers, and diagram PDFs
  puzzle          Mine, convert, tag, and summarize puzzle lines
  config          Show/validate configuration
  gui             Launch the GUI
  gui-web         Launch the chess-web-inspired GUI
  gui-next        Launch the Studio GUI v3
  doctor          Check Java, config, protocol, engine, and local artifacts
  clean           Delete session cache/logs
  help            Show command help

command style:
  Use noun groups plus actions, for example `crtk move list` or `crtk engine bestmove`.
  Prefer named flags for structured values: `--fen`, `--input`, `--output`, `--format`.
  Put options before free-form args when scripting, and use `--` if a value could look like a flag.

help:
  crtk --help                Show this summary
  crtk move --help           Show help for one area
  crtk move list --help      Show help for one command
  crtk help --full           Show the built-in command reference


```

### help_stats

```text
Unknown command for help: stats
crtk — ChessRTK (chess research toolkit)

usage:
  crtk <area> <action> [options] [args]
  crtk help [command...]

areas:
  record          Export, filter, split, and summarize .record files
  fen             Validate, normalize, generate, print, and transform FENs
  move            List, convert, and apply moves
  engine          Analyze, evaluate, search, and run movegen checks
  book            Render chess books, covers, and diagram PDFs
  puzzle          Mine, convert, tag, and summarize puzzle lines
  config          Show/validate configuration
  gui             Launch the GUI
  gui-web         Launch the chess-web-inspired GUI
  gui-next        Launch the Studio GUI v3
  doctor          Check Java, config, protocol, engine, and local artifacts
  clean           Delete session cache/logs
  help            Show command help

command style:
  Use noun groups plus actions, for example `crtk move list` or `crtk engine bestmove`.
  Prefer named flags for structured values: `--fen`, `--input`, `--output`, `--format`.
  Put options before free-form args when scripting, and use `--` if a value could look like a flag.

help:
  crtk --help                Show this summary
  crtk move --help           Show help for one area
  crtk move list --help      Show help for one command
  crtk help --full           Show the built-in command reference


```
